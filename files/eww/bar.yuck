; Includes
(include "./hyprland.yuck")
(include "./controls.yuck")
(include "./system.yuck")
(include "./clock.yuck")

; Overall widget. `screen` is the monitor name this bar belongs to; the left
; and center sections are per-monitor, the right section is global.
(defwidget bar [screen]
	(centerbox  :orientation "horizontal"
							:halign "fill"
		(left :screen screen)
		(center :screen screen)
		(right)
	)
)

; Left, center, right sections
(defwidget left [screen]
	(box  :orientation "h"
				:space-evenly false
				:halign "start"
				:class "section left"

		(workspaces :screen screen)
	)
)

(defwidget center [screen]
	(box  :orientation "h"
				:space-evenly false
				:halign "center"
				:class "section center ${(window_titles?.[screen] ?: "") == "" ? "empty" : "occupied" }"

		(active_window :screen screen)
	)
)

(defwidget right []
	(box  :orientation "h"
				:space-evenly false
				:halign "end"
				:class "section right"

		(systray
			:orientation 'h'
		)
		(sep)
		(controls)
		(sep)
		(system-info)
		(sep)
		(clock)
	)
)

; Right side stuff
(defwidget sep []
	(box  :class "separator"
		(label :text "|")
	)
)
