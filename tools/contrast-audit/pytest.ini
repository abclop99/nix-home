[pytest]
# tests/ has no __init__.py, so pytest's prepend import mode would put tests/
# itself on sys.path rather than its parent, and `import contrast_audit` would
# fail. pythonpath (pytest 7+) puts the package root on the path instead.
pythonpath = .
testpaths = tests
