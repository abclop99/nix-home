# Host-specific extra Hyprland binds. Skip-worktree'd: this committed placeholder
# is an empty list; real per-machine binds live only on disk. See CLAUDE.md
# "Private files" and modules/hyprland.nix (imports this as `priv.extraBinds`).
{
  extraBinds = [ ];
}
